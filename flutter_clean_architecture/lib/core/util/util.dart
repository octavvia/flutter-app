// util page

// Fungsi: Layer ini berisi fungsi-fungsi umum dan helper yang bisa digunakan di berbagai bagian aplikasi. Ini termasuk fungsi format tanggal, validasi input, konversi data, dan utilitas lain yang tidak spesifik terhadap satu fitur.

// contoh : 
// class DateUtils {
//   static String formatDate(DateTime date) {
//     final DateFormat formatter = DateFormat('yyyy-MM-dd');
//     return formatter.format(date);
//   }
// }

// class Validator {
//   static bool isEmail(String email) {
//     final RegExp regex = RegExp(r'^[^@]+@[^@]+\.[^@]+');
//     return regex.hasMatch(email);
//   }
// }
